import type { isArray } from "./bloated";

export {
  isArray,
}

export function map<T, U>(arr: Array<T>, fn: (item: T) => U): Array<U>;

export default function Lodash(): {
  map: typeof map;
  isArray: typeof isArray;
};
